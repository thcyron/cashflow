name: Run tests
on: [push]
jobs:
  main:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        go: ["1.15"]
    steps:
      - uses: actions/checkout@master
      - uses: actions/setup-go@v2
        with:
          go-version: ${{ matrix.go }}
      - run: go test -cover ./...
